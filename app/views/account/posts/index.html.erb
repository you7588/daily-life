<div class="jumbotron">
<h2 class="text-center">我发表过的言论<br><small>相互鼓励是一件难得的事情。</small></h2>
<div class="menu">
<i class="fa fa-home" aria-hidden="true">首页</i>
<i class="fa fa-clock-o" aria-hidden="true">时间轴</i>

<i class="fa fa-th-list" aria-hidden="true">分类</i>
<i class="fa fa-thumb-tack" aria-hidden="true">标签</i>
<i class="fa fa-clock-o" aria-hidden="true">关于本站</i>
</div>
</div>
<div class="col-md-10 col-md-offset-1">
  <h2 class="text-center">我发表过的留言</h2>

  <table class="table">
    <thead>
      <tr>
        <th>内容</th>
        <th>文章名</th>
        <th>最后更新</th>
        <th colspan="2">操作</th>
      </tr>
    </thead>

    <tbody>
      <% @posts.each do |post| %>
      <tr>
        <td><%= simple_format(post.content) %></td>
        <td> <%= post.orid.title %> </td>
        <td> <%= post.updated_at %> </td>
        <td> <%= link_to('Edit', edit_orid_post_path(post.orid, post), class: "btn btn-default btn-xs") %></td>
        <td> <%= link_to('Delete', orid_post_path(post.orid, post), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default btn-xs") %></td>
    </tr>
      <% end %>
    </tbody>
  </table>
</div>
